package com.quiz.ui.theme

import androidx.compose.ui.graphics.Color

// Primary greens (accent)
val PrimaryGreen = Color(0xFF65D36A)    // buttons, primary actions
val PrimaryGreenDark = Color(0xFF3EA14A) // pressed / darker accent
val PrimaryGreenLight = Color(0xFF9AF1A5) // highlights, subtle tints

// Backgrounds / surfaces (very dark)
val Background = Color(0xFF0E1112) // app background (near black)
val Surface = Color(0xFF151819)    // card / sheet backgrounds
val ElevatedSurface = Color(0xFF1A1E20) // slightly lighter surface for cards
val SurfaceVariant = Color(0xFF222526)

// Text colors (muted cool-gray for secondary text)
val OnBackground = Color(0xFFBFC8D0) // primary readable text (off-white)
val OnSurface = Color(0xFFBFC8D0)
val SecondaryText = Color(0xFF6F7A84) // lighter, for descriptions & subtitles
val Inactive = Color(0xFF5A666D) // for very muted elements (progress rail, outlines)

// Functional colors (optional)
val Success = Color(0xFF58C07A)
val Error = Color(0xFFEF5350)
val Outline = Color(0xFF2B3336) // for subtle borders around chips/choices